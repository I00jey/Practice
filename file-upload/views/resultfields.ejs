<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Result</title>
</head>

<body>
    <% for( let i=0; i < file1.length; i++ ) { %>
        <h2>
            <%= data[i] %>
        </h2>
        <img src="<%= file1[i].path %>" alt="">
        <% } %>

            <% for( let j=0; j < file1.length; j++ ) { %>
                <h2>
                    <%= data[j] %>
                </h2>
                <img src="<%= file1[j].path %>" alt="">
                <% } %>
</body>

</html>